services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    env_file: .env
    volumes: 
      - "volume:/var/lib/postgresql/data"
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - "db"
  
volumes:
  volume: {}